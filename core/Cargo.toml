[package]
name = "skill-docket-core"
version = "0.1.0"
edition = "2021"
description = "Skill Docket application daemon — orchestration, monitoring, agent lifecycle"

[dependencies]
skd-triggers = { path = "../../skd-triggers" }
ob-utils = { path = "/Users/oblinger/ob/proj/ob-utils/rs" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
libc = "0.2"
